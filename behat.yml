default:
    formatters:
        pretty:
            verbose:  true
            paths:    false
            snippets: false

    suites:
        default:
            contexts:
                - Context\FeatureContext

    extensions:
        Behat\MinkExtension:
            # turn on .htaccess on travis - AllowOverride All
            base_url:  'http://localhost/'
            sessions:
                default:
                    goutte: ~

