{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Channel list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Channel</th>
                <th>Length</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('channel_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td><a href="{{ path('channel_show', { 'id': entity.id }) }}">{{ entity.channel }}</a></td>
                <td>{{ entity.length }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('channel_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('channel_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('channel_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
