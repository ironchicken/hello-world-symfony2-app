{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Strait list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Length</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('strait_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td><a href="{{ path('strait_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td>{{ entity.length }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('strait_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('strait_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('strait_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
